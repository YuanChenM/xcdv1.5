spring:
  mail:
    from: "oms-uat <root@xianchida.com>"
    host: 10.45.21.248            #SMTP server host. For instance `smtp.example.com`
    username: root@xianchida.com  #Login user of the SMTP server.
    password: 123456              #Login password of the SMTP server.
    port: 25                      #SMTP server port
    proxy:
      enable: false
      proxyHost: 
      proxyPort: 
    properties:
      mail:
        smtp:
          auth: false
          ssl:
            enable: false
server:
  port: 8095
  context-path: /msk-mail/

#append.mail.cc: xxxxx